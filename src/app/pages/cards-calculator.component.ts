import { Component } from '@angular/core';

@Component({
  selector: 'app-cards-calculator',
  standalone: true,
  template: `
    <section class="page-shell">
      <p class="eyebrow">Calculator</p>
      <h1>Cards Calculator</h1>
      <p class="lead">
        Placeholder page for card calculations (drop rates, combinations, and bonuses).
      </p>
    </section>
  `
})
export class CardsCalculatorComponent {}
